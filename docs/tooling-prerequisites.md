# Tooling prerequisites

Required:

- AWS CLI v2
- Terraform
- kubectl
- Docker
- Git
- jq

Validate with:

```bash
aws --version
terraform version
kubectl version --client
docker --version
git --version
jq --version
```
