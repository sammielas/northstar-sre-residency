# Floci Platform

Local AWS-compatible development and training environment.

## Components

- `bootstrap/` — Terraform state bootstrap for Floci
- `terraform/` — Floci infrastructure configurations
- `eks/` — Floci EKS experiments and cluster definitions
- `scripts/` — Floci-specific validation and preflight scripts
