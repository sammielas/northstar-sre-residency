# AWS Platform

Production-oriented AWS infrastructure.

## Components

- `terraform/` — reusable AWS Terraform configurations
- `eks/` — real Amazon EKS configuration
- `production/` — production environment definitions
